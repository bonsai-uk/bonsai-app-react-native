import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    width: '86%',
  },
  logo: {
    width: 90,
    height: 90,
  },

  title: {
    marginTop: -18,
    color: '#23C686',
    fontSize: 64,
    fontWeight: '900',
  },

  subheading: {
    fontSize: 16,
    textAlign: 'center',
  },
});
